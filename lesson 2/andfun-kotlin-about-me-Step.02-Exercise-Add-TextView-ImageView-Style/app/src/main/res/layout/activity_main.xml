<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TODO (01) Add a TextView and style it and extract strings -->

    <!-- TODO (03) Create NameStyle, extract all dimensions -->

    <!-- TODO (04) Add ImageView with star image -->

    <TextView
        android:id="@+id/name_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/name_text_Text"
        android:textAlignment="center"
        style="@style/name_text_Style" />

    <ImageView
        android:id="@+id/star_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/star_image_ContentDes"
        app:srcCompat="@android:drawable/btn_star_big_on" />
</LinearLayout>