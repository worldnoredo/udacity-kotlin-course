<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="name_text_Style">
        <item name="android:layout_marginTop">@dimen/name_text_MarginTop</item>
        <item name="android:fontFamily">@font/roboto_light</item>
        <item name="android:paddingTop">@dimen/name_text_PaddingTop</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/name_text_TextSize</item>
    </style>

</resources>